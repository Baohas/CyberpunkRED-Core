{
  "mcpServers": {
    "playwright": {
      "command": "npx",
      "args": [
        "@playwright/mcp@latest",
        "--executable-path",
        "${PLAYWRIGHT_MCP_EXECUTABLE_PATH}",
        "--config",
        "${CLAUDE_PROJECT_DIR}/.cprc-shared/.playwright/playwright-mcp.config.json"
      ]
    }
  }
}
