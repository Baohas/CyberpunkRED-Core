{
  "type": "object",
  "additionalProperties": false,
  "properties": {
    "name": {
      "description": "Not supported in compendia, should be 0 length",
      "type": "string",
      "maxLength": 0
    },
    "uuid": {
      "description": "UUID of the Ammo used in a magazine. Not implemented in compendia so must be 0 length",
      "type": "string",
      "maxLength": 0
    }
  },
  "required": [
    "name",
    "uuid"
  ]
}
