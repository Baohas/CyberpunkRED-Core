{{cprTrace "START DIALOG ROLLS CPR-VERIFY-ROLL-SKILL-PROMPT.HBS"}}
<form>
    <div>
        {{localize "CPR.rolls.rollingFor"}} "{{rollData.rollTitle}}", Choomba!
    </div>

    <ol class="items-list">
        <li class="item flexrow">
            <div class="item flexrow setting-name text-nowrap">
                {{localize "CPR.global.generic.stat"}} {{localize "CPR.global.generic.value"}} ({{localize rollData.statName}})
            </div>
            <div class="item flexrow setting-value text-nowrap item-end">
                <input name="statValue" class="" type="number" data-dtype="Number" value={{rollData.statValue}}>
            </div>
        </li>

        {{#if (cprListContains prototypeChain "CPRSkillRoll")}}
        <li class="item flexrow">
            <div class="item flexrow setting-name text-nowrap">
                {{localize "CPR.global.itemTypes.skill"}} {{localize "CPR.global.generic.value"}} ({{localize rollData.skillName}})
            </div>
            <div class="item flexrow setting-value text-nowrap item-end">
                <input name="skillValue" class="" type="number" data-dtype="Number" value={{rollData.skillValue}}>
            </div>
        </li>
        {{/if}}

        {{#if roleValue}}
        <li class="item flexrow">
            <div class="item flexrow setting-name text-nowrap">
                {{localize "CPR.global.generic.role"}} {{localize "CPR.global.generic.value"}} ({{localize rollData.roleName}})
            </div>
            <div class="item flexrow setting-value text-nowrap item-end">
                <input name="roleValue" class="" type="number" data-dtype="Number" value={{rollData.roleValue}}>
            </div>
        </li>
        {{/if}}


        {{#if universalBonusAttack}}
        <li class="item flexrow">
            <div class="item flexrow setting-name text-nowrap">
                {{localize "CPR.universalBonuses.attack"}}:
            </div>
            <div class="item flexrow setting-value text-nowrap item-end">
                <input name="universalBonusAttack" class="" type="number" data-dtype="Number" value={{rollData.universalBonusAttack}}>
            </div>
        </li>
        {{/if}}


        <li class="item flexrow">
            <div class="item flexrow setting-name text-nowrap">
                {{localize "CPR.rolls.mods"}}:
            </div>
            <div class="item flexrow setting-value text-nowrap item-end" data-tooltip="
                {{#each rollData.mods}}
                  {{this.source}}: {{this.value}}
                  <br>
                {{/each}}
              ">
              <input disabled name="mods" class="" type="text" data-dtype="String" value="{{#each rollData.mods}}{{this.value}}{{#unless @last}}, {{/unless}}{{/each}}">
            </div>
        </li>

        <li class="item flexrow">
            <div class="item flexrow setting-name text-nowrap">
                {{localize "CPR.rolls.luck"}}:
            </div>
            <div class="item flexrow setting-value text-nowrap item-end">
                <input name="luck" class="" type="number" data-dtype="Number" value={{rollData.luck}}>
            </div>
        </li>

        <li class="item flexrow">
            <div class="item flexrow setting-name text-nowrap">
                {{localize "CPR.rolls.shouldRollCriticals"}}:
            </div>
            <div class="item flexrow setting-value text-nowrap item-end">
                <input name="calculateCritical" class="item-checkbox" type="checkbox" data-target = "calculateCritical" data-dtype="Checkbox" {{#if rollData.calculateCritical}} checked {{/if}}>
            </div>
        </li>

        {{#if (cprListContains prototypeChain "CPRAimedAttackRoll")}}
        <li class="item flexrow">
            <div class="item flexrow setting-name text-nowrap">
                {{localize "CPR.rolls.aimFor"}}:
            </div>
            <div class="item flexrow setting-value text-nowrap item-end">
                <select name="location">
                  {{#select rollData.location}}
                    {{#each (cprFindConfigObj "aimedLocation") as |location l|}}
                    <option value="{{l}}">{{localize location}}</option>
                    {{/each}}
                  {{/select}}
                </select>
            </div>
        </li>
        {{/if}}
    </ol>

    <hr>

    <ol class="items-list">
      {{#each activeEffects as |effect|}}
      {{#each effect.changes as |change|}}
      {{#if (cprCheckSituational ../../actor "cyberpunk-red-core" effect.id)}}
      <li class="item flexrow">
        <div class="item flexrow setting-name text-nowrap text-bold">
            {{effect.label}}:
        </div>
        <div class="item flexrow setting-value text-nowrap">
          {{cprGetChangeNameByKey ../this (lookup effect.flags.cyberpunk-red-core.changes @index) change.key}}
          {{#if change.value}}
              {{cprEffectModMode change.mode change.value}}
          {{else}}
              0
          {{/if}}
        </div>
        <div class="item flexrow setting-value text-nowrap item-end">
          <input {{checked (cprObjectListContains ../../rollData.mods "id" effect.id)}} name="{{change.key}}" class="active-effect-checkbox" type="checkbox" data-value = "{{change.value}}" data-source = "{{effect.label}}" data-mod-id = "{{effect.id}}" data-dtype="Checkbox">
        </div>
      </li>
      {{/if}}
      {{/each}}
      <br>
      {{/each}}
    </ol>

{{> "systems/cyberpunk-red-core/templates/dialog/cpr-dialog-buttons.hbs"}}

</form>
{{cprTrace "END CPR-VERIFY-ROLL-SKILL-PROMPT.HBS"}}
