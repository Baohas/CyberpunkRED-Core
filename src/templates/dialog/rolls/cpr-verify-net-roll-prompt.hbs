{{cprTrace "START DIALOG ROLLS CPR-VERIFY-NET-ROLL-PROMPT.HBS"}}
<form>
    <div>
        {{localize "CPR.rolls.rollingFor"}} "{{rollData.rollTitle}}", Choomba!
    </div>

    <ol class="items-list">
        {{#if rollData.statName}}
          <li class="item flexrow">
              <div class="item flexrow setting-name text-nowrap">
                  {{localize "CPR.global.generic.stat"}} {{localize "CPR.global.generic.value"}} ({{localize rollData.statName}})
              </div>
              <div class="item flexrow setting-value text-nowrap item-end">
                  <input name="statValue" class="" type="number" data-dtype="Number" value={{rollData.statValue}}>
              </div>
          </li>
        {{/if}}

        {{#if rollData.roleName}}
        <li class="item flexrow">
            <div class="item flexrow setting-name text-nowrap">
                {{localize "CPR.rolls.abilityRank"}} ({{localize rollData.roleName}})
            </div>
            <div class="item flexrow setting-value text-nowrap item-end">
                <input name="roleValue" class="" type="number" data-dtype="Number" value={{rollData.roleValue}}>
            </div>
        </li>
        {{/if}}

        {{!-- All applied modifier list partial --}}
        {{> "systems/cyberpunk-red-core/templates/dialog/rolls/cpr-all-modifiers.hbs" this}}

        {{#if (cprCompare (cprCompare rollData.rollCardExtraArgs.program.class "!==" "blackice") "&&" (cprCompare rollData.rollCardExtraArgs.program.class "!==" "demon"))}}
        <li class="item flexrow">
            <div class="item flexrow setting-name text-nowrap">
                {{localize "CPR.rolls.luck"}}:
            </div>
            <div class="item flexrow setting-value text-nowrap item-end">
                <input name="luck" class="" type="number" data-dtype="Number" value={{rollData.luck}}>
            </div>
        </li>
        {{/if}}

        <li class="item flexrow">
            <div class="item flexrow setting-name text-nowrap">
                {{localize "CPR.rolls.shouldRollCriticals"}}:
            </div>
            <div class="item flexrow setting-value text-nowrap item-end">
                <input name="calculateCritical" class="" type="checkbox" data-dtype="Checkbox" {{checked rollData.calculateCritical}}>
            </div>
        </li>
    </ol>

    {{!-- Situational modifier list partial --}}
    {{#if (cprCompare filteredMods.length ">" 0)}}
      {{> "systems/cyberpunk-red-core/templates/dialog/rolls/cpr-situational-modifiers.hbs" this}}
    {{/if}}

  {{> "systems/cyberpunk-red-core/templates/dialog/cpr-dialog-buttons.hbs"}}

</form>
{{cprTrace "END CPR-VERIFY-NET-ROLL-PROMPT.HBS"}}
