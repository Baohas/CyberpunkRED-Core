{{cprTrace "START DIALOG ROLLS CPR-VERIFY-PROGRAM-DAMAGE-PROMPT.HBS"}}
<form>
  {{log this}}
    <div>
        {{localize "CPR.rolls.roll"}} {{localize "CPR.rolls.damageFor"}}
        {{#if rollCardExtraArgs.program.name}}
          "{{rollCardExtraArgs.program.name}} ({{localize (cprFindConfigValue "programClassList" rollCardExtraArgs.pgmClass)}})", Choomba!
        {{else}}
          "{{localize "CPR.global.role.netrunner.interfaceAbility.zap"}}"
        {{/if}}
    </div>
    <ol class="items-list">
      {{#if (cprCompare (cprCompare rollCardExtraArgs.pgmClass "===" "antiprogramattacker") "&&" (cprCompare rollCardExtraArgs.pgmDamage.blackIce "!==" "")) }}
        <li class="item flexrow">
            <div class="item flexrow setting-name text-nowrap">
                {{localize "CPR.rolls.selectTarget"}}:
            </div>
            <div class="item flexrow setting-value text-nowrap item-end">
              <select name="formula">
                    <option selected value="{{rollCardExtraArgs.pgmDamage.standard}}">{{localize "CPR.itemSheet.program.damageTo"}} {{localize "CPR.itemSheet.program.nonBlackIce"}} ({{rollCardExtraArgs.pgmDamage.standard}})</option>
                    <option value="{{rollCardExtraArgs.pgmDamage.blackIce}}">{{localize "CPR.itemSheet.program.damageTo"}} {{localize "CPR.global.programClass.blackice"}} ({{rollCardExtraArgs.pgmDamage.blackIce}})</option>
                </select>
            </div>
        </li>
        {{/if}}
         <li class="item flexrow">
            <div class="item flexrow setting-name text-nowrap">
                {{localize "CPR.rolls.mods"}}:
            </div>
            <div class="item flexrow setting-value text-nowrap item-end">
                <input name="mods" class="" type="text" data-dtype="String" value={{mods}}>
            </div>
        </li>
    </ol>
</form>
{{cprTrace "END CPR-VERIFY-PROGRAM-DAMAGE-PROMPT.HBS"}}
