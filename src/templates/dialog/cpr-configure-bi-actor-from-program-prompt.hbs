{{cprTrace "START DIALOG CPR-CONFIGURE-BI-ACTOR-FROM-PROGRAM-PROMPT.HBS"}}
<form>
  <div>
    <ol class="items-list">
      <li class="item flexrow">
        <div class="item flexrow">
          <select name="programUUID">
            <option value = "unlink">{{localize "CPR.dialog.configureBlackIceActorFromProgram.unlinkBlackIceFromProgram"}}</option>
            {{#each biProgramList as |program|}}
              <option value="{{program.uuid}}">{{#if (cprCompare program.uuid "===" linkedProgramUUID)}}* {{/if}}{{program.name}}</option>
            {{/each}}
          </select>
        </div>
      </li>
    </ol>
  </div>
</form>
{{cprTrace "END CPR-CONFIGURE-BI-ACTOR-FROM-PROGRAM-PROMPT.HBS"}}

